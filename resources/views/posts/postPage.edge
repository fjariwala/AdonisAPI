<h1>{{title}}</h1>

@if(old('notification'))

<div class="alert alert-danger">
    {{old('notification')}}
</div>
  
@endif

<ul>
    @each(post in posts)
    <li>
        {{post.title}}
    </li>
    @endeach
</ul>